# KnowledgeFlow - 基于 OpenAgents 的智能知识内容助手

> 一个多 Agent 协作系统，实现**信息采集 → 智能处理 → AI 创作**的完整工作流

<div align="center">

[![OpenAgents](https://img.shields.io/badge/OpenAgents-0.6.13+-blue.svg)](https://github.com/OpenAgentsInc/openagents)
[![Python](https://img.shields.io/badge/Python-3.11+-green.svg)](https://www.python.org/)
[![License](https://img.shields.io/badge/License-MIT-yellow.svg)](LICENSE)

[快速开始](#-快速开始) • [系统架构](#-系统架构) • [核心功能](#-核心功能规划) • [使用指南](#-使用指南)

</div>

---

## 📖 项目简介

**KnowledgeFlow** 是一个基于 [OpenAgents](https://github.com/OpenAgentsInc/openagents) 框架构建的多 Agent 协作系统，旨在帮助用户自动化地采集、处理和创作知识内容。

### 🎯 核心价值

- ⚡ **自动化工作流**：从信息采集到内容生成的全流程自动化
- 🤖 **多 Agent 协作**：10 个专业 Agent 各司其职，事件驱动协同工作
- 🧠 **智能处理**：AI 自动摘要、标签分类、多视角评审
- 📊 **可视化监控**：通过 OpenAgents Studio 实时查看系统状态
- 🔄 **灵活扩展**：基于 Mod 系统的模块化架构

---

## 🏗️ 系统架构

### 四层 Agent 网络

```
┌─────────────────────────────────────────┐
│         📥 采集层 (Collection)          │
├─────────────────────────────────────────┤
│  ① RSS Reader      │  ② Web Scraper     │
│  自动订阅采集      │  手动URL抓取       │
└──────────┬──────────────────────────────┘
           │ Event: content.discovered
           ▼
┌─────────────────────────────────────────┐
│         🔍 处理层 (Processing)          │
├─────────────────────────────────────────┤
│  ③ Summarizer      │  ④ Tagger          │
│  AI智能摘要        │  自动标签分类      │
└──────────┬──────────────────────────────┘
           │ Event: content.tagged
           ▼
┌─────────────────────────────────────────┐
│         ✍️ 创作层 (Creation)            │
├─────────────────────────────────────────┤
│  ⑤ Outline Gen.    │  ⑥ Writer          │
│  大纲生成器        │  文章写作器        │
│  ⑦ Creation Coordinator (协调器)       │
└──────────┬──────────────────────────────┘
           │ Event: creation.draft_ready
           ▼
┌─────────────────────────────────────────┐
│         👥 评审层 (Review)              │
├─────────────────────────────────────────┤
│  ⑧ Critic Business │  ⑨ Critic Tech    │
│  商业视角评审      │  技术视角评审      │
│  ⑩ Critic User (用户视角评审)          │
└─────────────────────────────────────────┘
           ▼
    OpenAgents Studio (人机协作界面)
```

### 🔌 扩展 Mods

```
┌─────────────────────────────────────────┐
│           📦 Network Mods               │
├─────────────────────────────────────────┤
│  • RSS Manager Mod     - RSS源管理      │
│  • Content Stats Mod   - 内容统计分析   │
│  • Creation Tracker    - 创作流程追踪   │
└─────────────────────────────────────────┘
```

---

## 🎯 核心功能规划

### Agent 功能详细说明

#### ① RSS Reader Agent（RSS订阅采集器）
**职责**：自动从配置的 RSS 订阅源采集技术文章和博客内容

- 🔄 **定时自动采集**：每 30 分钟自动检查配置的 RSS 源
- 📡 **多源支持**：支持配置多个 RSS 源（HackerNews、Medium、技术博客等）
- 🔍 **智能去重**：自动检查 URL 避免重复采集
- 📊 **内容过滤**：过滤过短或无效的内容
- 📤 **事件发送**：采集完成后发送 `content.discovered` 事件通知下游 Agent
- 💬 **频道通知**：在「灵感捕手」频道展示新采集的内容预览

**实现文件**：`agents/rss_reader.py`  
**配置文件**：`config/rss_feeds.yaml`

---

#### ② Web Scraper Agent（网页内容抓取器）
**职责**：根据用户指定的 URL 抓取网页正文内容

- 🎯 **按需抓取**：监听「灵感采集」频道，响应用户发送的 URL
- 📄 **全文提取**：使用 trafilatura 提取干净的正文内容
- 🧹 **内容清洗**：自动去除广告、导航等无关内容
- ✅ **URL验证**：检查 URL 格式和可访问性
- 🔄 **去重检查**：避免重复抓取已存在的内容
- 📤 **事件发送**：抓取完成后发送 `content.discovered` 事件
- 💬 **即时反馈**：在频道中回复抓取结果

**实现文件**：`agents/web_scraper.py`  
**使用方式**：在 Studio 的「灵感采集」频道发送 URL 即可

---

#### ③ Summarizer Agent（AI摘要生成器）
**职责**：为采集到的内容生成多层级智能摘要

- 📝 **三级摘要**：
  - 一句话摘要（10-15字）：快速了解核心内容
  - 段落摘要（50-80字）：概括主要观点
  - 详细摘要（150-200字）：完整的内容梗概
- 🎯 **关键提取**：
  - 3-5 个核心要点
  - 1-3 条重要引用
- 🤖 **LLM驱动**：使用 OpenAI GPT-4 模型生成高质量摘要
- ⚡ **事件驱动**：监听 `content.discovered` 事件自动处理
- 📤 **事件发送**：处理完成后发送 `content.summarized` 事件

**实现文件**：`agents/summarizer.py`  
**提示词配置**：`config/prompts/summarize.py`

---

#### ④ Tagger Agent（智能标签分类器）
**职责**：为内容自动打标签并进行多维度分类

- 🏷️ **智能标签**：根据内容自动生成 3-5 个相关标签
- 📂 **多级分类**：
  - **主题分类**：AI、Tech、Business、Science、Other
  - **难度等级**：初级、中级、高级
  - **内容类型**：教程、评测、思考、新闻等
- 🌍 **语言检测**：自动识别中文、英文等语言
- 🎨 **情感分析**：分析内容的情感倾向（正面/中性/负面）
- ⚡ **事件驱动**：监听 `content.summarized` 事件
- 📤 **事件发送**：处理完成后发送 `content.tagged` 事件
- 💬 **频道展示**：在「知识库」频道展示处理完成的内容卡片

**实现文件**：`agents/tagger.py`  
**提示词配置**：`config/prompts/tag.py`

---

#### ⑤ Outline Generator Agent（大纲生成器）
**职责**：根据主题和知识库内容生成文章大纲

- 💡 **多方案生成**：为同一主题生成 2-3 个不同角度的大纲
- 🔍 **知识库检索**：从已采集的内容中搜索相关素材
- 📋 **结构化输出**：
  - 清晰的章节层级
  - 每个章节的核心要点
  - 关联的参考资料
- 🎨 **多种风格**：
  - 技术深度分析
  - 实践操作指南
  - 行业趋势综述
- ⚡ **事件驱动**：监听 `creation.request_outlines` 事件
- 📤 **事件发送**：生成完成后发送 `creation.outlines_ready` 事件
- 💬 **交互式选择**：在「创作工坊」频道展示大纲供用户选择

**实现文件**：`agents/outline_generator.py`  
**提示词配置**：`config/prompts/outline.py`

---

#### ⑥ Writer Agent（AI内容写作器）
**职责**：基于大纲和知识库生成完整文章

- ✍️ **AI写作**：根据选定的大纲展开完整文章内容
- 📚 **引用整合**：自动引用知识库中的相关内容和数据
- 🎨 **风格控制**：
  - 技术深度（入门/中级/深度）
  - 语言风格（严谨/轻松/口语化）
  - 文章长度（1000/2000/3000字）
- 🔗 **自动引用**：在文末添加参考资料链接
- ⚡ **事件驱动**：监听 `creation.start_writing` 事件
- 📤 **事件发送**：写作完成后发送 `creation.draft_ready` 事件
- 💬 **频道输出**：在「创作工坊」频道展示完整文章

**实现文件**：`agents/writer.py`  
**提示词配置**：`config/prompts/write.py`

---

#### ⑦ Creation Coordinator（创作协调器）
**职责**：协调整个内容创作流程，管理用户交互

- 🎭 **状态机管理**：维护创作流程的各个状态
- 💬 **用户交互**：处理「创作工坊」频道的用户消息
- 🔄 **流程编排**：
  1. 接收用户创作请求
  2. 触发大纲生成
  3. 展示大纲供用户选择
  4. 触发文章写作
  5. 展示最终文章
- 📤 **事件协调**：
  - 发送 `creation.request_outlines` 事件
  - 监听 `creation.outlines_ready` 事件
  - 发送 `creation.start_writing` 事件
  - 监听 `creation.draft_ready` 事件
- 🎯 **意图识别**：智能解析用户的创作需求

**实现文件**：`agents/creation_coordinator.py`

---

#### ⑧ Critic Business（商业视角评审Agent）
**职责**：从商业角度评审文章内容

- 💼 **商业价值**：评估内容的商业价值和市场潜力
- 📊 **受众分析**：分析目标受众和市场定位
- 💰 **变现潜力**：评估内容的商业化可能性
- 🎯 **建议输出**：提供商业优化建议
- ⚡ **事件驱动**：监听 `creation.draft_ready` 事件

**实现文件**：`agents/critic_business.py`  
**提示词配置**：`config/prompts/critic_business.py`

---

#### ⑨ Critic Technical（技术视角评审Agent）
**职责**：从技术角度评审文章内容

- 🔬 **技术准确性**：检查技术细节和代码示例的正确性
- 📐 **逻辑严谨性**：评估论述逻辑和推理过程
- 📚 **深度评估**：判断技术深度是否符合目标读者水平
- 💡 **改进建议**：提供技术优化和补充建议
- ⚡ **事件驱动**：监听 `creation.draft_ready` 事件

**实现文件**：`agents/critic_technical.py`
**提示词配置**：`config/prompts/critic_technical.py`

---

#### ⑩ Critic User（用户视角评审Agent）
**职责**：从用户体验角度评审内容

- 👥 **可读性**：评估内容的易读性和流畅度
- 🎯 **价值感知**：分析用户能获得的实际价值
- 📱 **使用体验**：评估排版、结构等用户体验要素
- 💬 **互动性**：建议增强用户参与度的方式
- ⚡ **事件驱动**：监听 `creation.draft_ready` 事件

**实现文件**：`agents/critic_user.py`
**提示词配置**：`config/prompts/critic_user.py`

---

## 🔄 Agent 协作工作流

### 工作流 1：自动采集 → 智能处理

```
RSS Reader (定时触发)
    ↓ 每30分钟自动采集
    ├─ 保存到数据库
    ├─ 发送到「灵感捕手」频道
    └─ 触发事件: content.discovered
            ↓
Summarizer (监听事件)
    ↓ AI生成三级摘要
    ├─ 更新数据库
    └─ 触发事件: content.summarized
            ↓
Tagger (监听事件)
    ↓ AI自动分类打标签
    ├─ 更新数据库
    ├─ 发送到「知识库」频道
    └─ 触发事件: content.tagged
            ↓
    ✅ 内容处理完成，进入知识库
```

### 工作流 2：手动抓取 → 智能处理

```
用户在「灵感采集」频道发送URL
    ↓
Web Scraper (监听频道消息)
    ↓ 抓取网页内容
    ├─ 保存到数据库
    ├─ 回复抓取结果
    └─ 触发事件: content.discovered
            ↓
    (进入工作流1的处理流程)
```

### 工作流 3：AI 创作 → 多视角评审

```
用户在「创作工坊」频道发起创作请求
    ↓
Creation Coordinator (监听频道消息)
    ↓ 解析创作需求
    └─ 触发事件: creation.request_outlines
            ↓
Outline Generator (监听事件)
    ↓ 从知识库检索素材
    ↓ 生成2-3个大纲方案
    ├─ 发送到「创作工坊」频道
    └─ 触发事件: creation.outlines_ready
            ↓
Creation Coordinator (监听事件)
    ↓ 展示大纲供用户选择
    ↓
用户选择大纲方案
    ↓
Creation Coordinator (解析用户选择)
    └─ 触发事件: creation.start_writing
            ↓
Writer (监听事件)
    ↓ 基于大纲生成完整文章
    └─ 触发事件: creation.draft_ready
            ↓
    ┌───────┴───────┬───────────┐
    ↓               ↓           ↓
Critic Business  Critic Tech  Critic User
(商业评审)      (技术评审)   (用户评审)
    ↓               ↓           ↓
    └───────┬───────┴───────────┘
            ↓
Creation Coordinator (汇总评审)
    ↓ 展示文章和评审结果
    └─ 发送到「创作工坊」频道
            ↓
    ✅ 创作完成，用户可查看和编辑
```

---

## 🔑 OpenAgents 关键特性应用

本项目充分利用了 OpenAgents 框架的核心能力：

### 1. 事件驱动系统（Event System）

Agent 间通过事件实现解耦通信，参考 `docs/4Event System.md`：

```python
# 发送事件
from openagents.models.event import Event

event = Event(
    event_name="content.discovered",
    source_id=self.agent_id,
    payload={
        "content_id": content_id,
        "title": title,
        "url": url
    }
)
await self.send_event(event)

# 监听事件
from openagents.agents.worker_agent import on_event

@on_event("content.discovered")
async def handle_content_discovered(self, event):
    payload = event.payload
    content_id = payload.get("content_id")
    # 处理事件...
```

**项目使用的事件流**：
- `content.discovered` → `content.summarized` → `content.tagged`
- `creation.request_outlines` → `creation.outlines_ready`
- `creation.start_writing` → `creation.draft_ready`

### 2. 频道消息系统（Channel Messaging）

通过 Messaging Mod 实现用户交互：

```python
# 发送频道消息
messaging = self.client.mod_adapters.get("openagents.mods.workspace.messaging")
await messaging.send_channel_message(
    channel="知识库",
    text="📚 新内容已添加"
)

# 监听频道消息
@on_event("thread.channel_message.notification")
async def handle_channel_message(self, event):
    channel = event.payload.get("channel")
    message = event.payload.get("content", {}).get("text", "")
    # 处理用户消息...
```

**项目使用的频道**：
- **通用频道** - 系统通知和状态公告
- **灵感捕手** - RSS和网页采集的新内容
- **知识库** - 已处理的内容（含摘要和标签）
- **创作工坊** - 内容创作交互空间
- **灵感采集** - 用户发送 URL 请求抓取

### 3. Network Mods（模块化扩展）

参考 `docs/6Network Mods.md`，通过 Mod 系统扩展能力：

```yaml
# network.yaml
mods:
  - name: "openagents.mods.workspace.messaging"
    config:
      default_channels: [...]

  - name: "rss_manager"
    type: "local"
    path: "./mods/rss_manager"
```

### 4. WorkerAgent 基类

所有 Agent 继承 WorkerAgent，获得标准能力：

```python
from openagents.agents.worker_agent import WorkerAgent, on_event

class MyAgent(WorkerAgent):
    default_agent_id = "my-agent"

    async def on_startup(self):
        # Agent 启动时执行
        pass

    async def on_shutdown(self):
        # Agent 关闭时执行
        pass
```

### 5. 中心化网络拓扑

采用中心化模式，参考 `docs/3 Architecture.md`：

```yaml
network:
  mode: "centralized"
  transports:
    - type: "http"
      config:
        port: 8700
        serve_studio: true
    - type: "grpc"
      config:
        port: 8600
```

**优势**：
- 统一的事件路由和管理
- 集中式监控
- 内置 OpenAgents Studio 界面
- 简化的部署配置

---

## 🚀 快速开始

### 第一步：环境准备

```bash
# 1. 创建 Python 虚拟环境（推荐 Python 3.11+）
conda create -n openagents python=3.11
conda activate openagents

# 2. 安装依赖
pip install -r requirements.txt
pip install openagents-sdk

# 3. 验证安装
openagents --version
```

### 第二步：配置 API Key

```bash
# 1. 复制配置文件
cp .env.example .env

# 2. 编辑 .env 文件，填入你的 OpenAI API Key
# OPENAI_API_KEY=sk-your-actual-api-key-here
# OPENAI_API_BASE=https://api.openai.com/v1  # 可选
```

### 第三步：一键启动

```bash
# 使用 tmux 启动所有服务（推荐）
./start_all.sh

# 这将自动启动：
# ✅ Network Server (http://localhost:8700)
# ✅ 10 个 AI Agent（自动协作）
```

### 第四步：访问 OpenAgents Studio

打开浏览器访问：**http://localhost:8700**

你将看到：
- 📊 所有 Agent 的运行状态
- 💬 实时频道消息流
- ✍️ 与 Agent 交互的界面

---

## 📖 使用指南

### 场景 1：配置 RSS 自动采集

#### 1. 编辑 RSS 配置

编辑 `config/rss_feeds.yaml`：

```yaml
feeds:
  - name: "Hacker News"
    url: "https://hnrss.org/frontpage"
    category: "tech-news"
    enabled: true

  - name: "阮一峰的网络日志"
    url: "http://www.ruanyifeng.com/blog/atom.xml"
    category: "tech-blog"
    enabled: true
```

#### 2. 重启 RSS Reader

```bash
# 使用 tmux
tmux attach -t knowledgeflow
# 按 Ctrl+B 然后按 1，切换到 RSS Reader 窗口
# 按 Ctrl+C 停止，然后运行：
python agents/rss_reader.py
```

#### 3. 查看采集结果

- 在 Studio 的「灵感捕手」频道查看新采集的内容
- 在「知识库」频道查看处理完成的内容（含摘要和标签）

### 场景 2：手动抓取网页

#### 方式 A：通过 OpenAgents Studio

1. 打开 http://localhost:8700
2. 进入「灵感采集」频道
3. 发送消息：
   ```
   https://openai.com/blog/gpt-4
   ```
4. Web Scraper 自动抓取并处理

#### 方式 B：通过命令行

```bash
# 直接调用 Web Scraper
python -c "
import asyncio
from agents.web_scraper import WebScraperAgent

async def main():
    agent = WebScraperAgent()
    await agent.scrape_url('https://openai.com/blog/gpt-4')

asyncio.run(main())
"
```

### 场景 3：AI 辅助创作

#### 1. 发起创作请求

在「创作工坊」频道发送：
```
帮我写一篇关于 GPT-4 技术解析的文章
```

#### 2. 选择大纲

系统会生成 2-3 个大纲方案，回复数字选择：
```
选择 1
```

#### 3. 查看文章和评审

- Writer 生成完整文章
- 三个 Critic Agent 同时进行多视角评审
- 在频道中查看最终结果

---

## 🗄️ 数据库配置

### SQLite 数据库

项目使用 SQLite 存储内容数据：

```python
# config/database.py
DATABASE_PATH = "data/knowledge.db"
```

### 数据表结构

```sql
-- 内容表
CREATE TABLE contents (
    id TEXT PRIMARY KEY,
    title TEXT NOT NULL,
    url TEXT UNIQUE,
    content TEXT,
    source TEXT,
    created_at TIMESTAMP,
    -- 摘要字段
    summary_short TEXT,
    summary_medium TEXT,
    summary_long TEXT,
    key_points TEXT,  -- JSON
    -- 标签字段
    tags TEXT,  -- JSON
    category TEXT,
    difficulty TEXT,
    content_type TEXT,
    language TEXT,
    sentiment TEXT
);

-- 创作记录表
CREATE TABLE creations (
    id TEXT PRIMARY KEY,
    topic TEXT,
    outlines TEXT,  -- JSON
    selected_outline INTEGER,
    article TEXT,
    reviews TEXT,  -- JSON
    status TEXT,
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);
```

### 数据库管理

```bash
# 查看数据库
sqlite3 data/knowledge.db

# 常用查询
.tables                          # 查看所有表
SELECT COUNT(*) FROM contents;   # 统计内容数量
SELECT title, tags FROM contents LIMIT 10;  # 查看最近内容
```

---

## 🔧 管理和监控

### 查看运行状态

```bash
# 查看所有 tmux 会话
tmux ls

# 进入主会话
tmux attach -t knowledgeflow

# 切换窗口（在 tmux 中）
Ctrl+B 然后按数字键 (0-9)
```

### 查看日志

```bash
# 查看特定 Agent 日志
tail -f logs/rss_reader.log
tail -f logs/summarizer.log

# 查看所有日志
tail -f logs/*.log
```

### 停止服务

```bash
# 停止所有服务
./stop_all.sh

# 或手动停止
tmux kill-session -t knowledgeflow
```

### 重启单个 Agent

```bash
# 进入 tmux 会话
tmux attach -t knowledgeflow

# 切换到对应窗口，Ctrl+C 停止后重新运行
python agents/xxx_agent.py
```

---

## ❓ 故障排查

### 常见问题

#### 1. Agent 无法连接到 Network

```bash
# 检查 Network 是否运行
curl http://localhost:8700/health

# 重启 Network
openagents network start --config network.yaml
```

#### 2. OpenAI API 调用失败

```bash
# 检查 API Key
echo $OPENAI_API_KEY

# 测试 API 连接
python -c "
import openai
client = openai.OpenAI()
print(client.models.list())
"
```

#### 3. RSS 采集无内容

- 检查 `config/rss_feeds.yaml` 中的 URL 是否有效
- 确认网络可以访问 RSS 源
- 查看 `logs/rss_reader.log` 中的错误信息

#### 4. 数据库锁定错误

```bash
# 检查是否有多个进程访问数据库
lsof data/knowledge.db

# 如果需要，删除锁文件
rm -f data/knowledge.db-journal
```

### 获取帮助

- 查看 OpenAgents 文档：`docs/` 目录
- 提交 Issue：[GitHub Issues](https://github.com/your-repo/issues)

---

## 🤝 贡献指南

欢迎贡献代码和建议！

### 开发流程

1. Fork 本仓库
2. 创建功能分支：`git checkout -b feature/your-feature`
3. 提交更改：`git commit -m 'Add your feature'`
4. 推送分支：`git push origin feature/your-feature`
5. 创建 Pull Request

### 代码规范

- 使用 Python 3.11+ 语法
- 遵循 PEP 8 代码风格
- 添加适当的类型注解
- 编写单元测试

### 添加新 Agent

1. 在 `agents/` 目录创建新文件
2. 继承 `WorkerAgent` 基类
3. 实现 `on_startup` 和事件处理方法
4. 在 `network.yaml` 中注册 Agent
5. 更新文档

---

## 📄 许可证

本项目采用 MIT 许可证 - 详见 [LICENSE](LICENSE) 文件

---

<div align="center">

**Built with [OpenAgents](https://github.com/OpenAgentsInc/openagents)**

</div>
